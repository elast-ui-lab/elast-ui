import React from "react";
import { SwitchProps } from "./types";
declare const Switch: ({ id, className, checked, children, onValueChange, }: SwitchProps) => React.JSX.Element;
export default Switch;
